// Layout for compare page with metadata
export const metadata = {
  title: "Compare Lists | sort(id)",
  description: "Compare your lists side-by-side to see overlapping picks and unique choices.",
};

export default function CompareLayout({ children }) {
  return children;
}
