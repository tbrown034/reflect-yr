// Layout for create page with metadata
export const metadata = {
  title: "Create a List | sort(id)",
  description: "Create a new ranked list of your favorite movies, TV shows, books, podcasts, or custom items.",
};

export default function CreateLayout({ children }) {
  return children;
}
